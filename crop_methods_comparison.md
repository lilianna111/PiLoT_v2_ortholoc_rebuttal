# 三种 Crop 方法对比分析

## 对比对象

本次对比的入口文件是：

- `crop/test.py`
- `crop_v2/test.py`
- `crop_wgs84_0329/test.py`

对应的核心实现分别位于：

- `crop/crop/`
- `crop_v2/crop/`
- `crop_wgs84_0329/crop/`

## 先说结论

严格按代码实现看，这三个入口并不是三套完全独立的方法，而是：

1. `crop`
   和
   `crop_v2`
   本质上是同一套核心裁剪方法。
2. `crop_wgs84_0329`
   才是方法层面真正明显不同的一版。

也就是说，如果按“核心算法”分类，其实更接近：

- 一类是 `crop` / `crop_v2`
- 一类是 `crop_wgs84_0329`

其中 `crop` 和 `crop_v2` 的以下核心文件是逐字一致的：

- `proj2map.py`
- `ray_casting.py`
- `transform_colmap.py`

这说明两者的主要差别不在算法本身，而在：

- 默认相机内参 `K.json`
- 测试脚本输出内容
- README 中给出的使用样例

## 三者共同点

虽然实现路径不同，但三者有一批共同基础：

### 1. 输入数据格式一致

三者都使用：

- DSM 栅格
- DOM 栅格
- 位姿 `pose_data = [lon, lat, alt, roll, pitch, yaw]`
- 相机内参 `K`

### 2. 坐标主线一致

三者都基于 WGS84 经纬度输入，并且都把世界坐标统一到 ECEF：

- 输入位姿是 WGS84 下的 `lon/lat/alt`
- 内部相机位姿通过 `transform_colmap_pose_intrinsic(...)` 转成 ECEF 下的 `pose_w2c`
- 最终点云输出都是 ECEF XYZ

也就是说，三者在“相机位姿如何从经纬高转成可投影矩阵”这件事上是相同的。

### 3. 相机姿态预处理一致

三者都使用同一套姿态预处理：

- `yaw_proc = -yaw`
- `pitch_proc = pitch - 90`
- `roll` 直接保留

再通过局部 ENU 到 ECEF 的旋转，构造相机外参。

### 4. 最终目标一致

三者都在做同一件事：

- 根据无人机位姿和相机内参，在地理地图上找到参考区域
- 从 DOM / DSM 中裁出与图像视角对应的一块区域
- 生成裁剪图像和对应的 3D 点信息

## `crop` 方法分析

### 方法核心

`crop` 的核心思路是：

1. 取图像四个角点
2. 对每个角点准备 3 个候选像素位置
   - 原始角点
   - 向图像中心缩 1/8
   - 向图像中心缩 1/4
3. 对每个候选点做射线投射，和 DSM 求交
4. 得到四个落地角点后，在 DSM 上形成一个四边形
5. 对这个四边形做透视变换，拉成规则矩形输出

### 它是怎么求交的

它不是直接把整块区域投影到地图，而是先解决“四个角落打到哪里”这个问题。

具体做法：

1. 从像素点反投影出相机射线
2. 在 ECEF 中沿射线采样
3. 每个采样点转换回经纬高
4. 去 DSM 中插值取高程
5. 比较“射线高度”和“DSM 高度”
6. 如果出现过零，说明射线穿过了地表，在局部区间做二分逼近

如果没有精确交点：

- 优先返回“最接近 DSM 的有效采样点”
- 如果整条线上都没有有效 DSM，就退化到一个 `fallback_height` 平面

### 它的 fallback 逻辑

`crop` 使用的是：

- DSM 中所有有效且 `> 0` 的高度最小值

这个值作为“兜底平面高度”。

这意味着：

- 如果角点打不到 DSM，它会假设该角点落在一个“全局最小有效高度平面”上
- 这种做法能保证方法不容易直接报错
- 但在高低起伏大、地形变化明显的区域，几何上可能偏保守，甚至偏大

### 裁剪方式

四个角点命中后，`crop` 的裁剪流程是：

1. 用四个命中点的 DSM 行列号求包围盒
2. 从 DSM 和 DOM 中先裁出一个轴对齐窗口
3. 在这个窗口内部，把四边形通过单应矩阵 `H` 做透视拉正
4. 输出规则矩形的 DOM 图像
5. 同时把高程网格和经纬度网格也一起做透视变换
6. 再把 `(lon, lat, h)` 转成 ECEF XYZ

所以它的本质是：

- 先找到真实落地四边形
- 再把四边形“重采样成矩形”

### 输出特点

`crop` 主要输出：

- 裁正后的 DOM 图像
- 对应的 ECEF 点云数组

默认保存为：

- `{name}_dom.png`
- `{name}_dsm.npy`

调试图层代码存在，但默认没有启用保存。

### 优点

- 四个角点是沿真实相机射线去找 DSM 的，几何含义比较直接
- 在地形真实起伏能被 DSM 表达时，角点落地更贴近真实视锥边界
- 输出是已经透视拉正后的规则矩形，后续使用方便

### 局限

- 只依赖四个角点来决定整块裁剪区域，区域形状完全由四点决定
- 如果某个角点求交不稳定，会影响整个变换结果
- fallback 用“全局最小有效高度”，对复杂地形可能过粗
- 没有显式输出 mask，DSM 空洞和矩形外区域没有单独区分
- 透视 warp 后的每个像素点是重采样结果，不再保留原始地图网格的像素对齐关系

## `crop_v2` 方法分析

### 核心结论

`crop_v2` 和 `crop` 在核心方法上没有区别。

它们的：

- 射线求交逻辑相同
- 四角点候选机制相同
- fallback 逻辑相同
- 透视变换方式相同
- 输出结构相同

### 它相对 `crop` 的变化

主要是工程层面的变化，而不是方法变化：

1. 默认内参和图像尺寸换了
   - `crop/K.json` 对应 `1920 x 1080`
   - `crop_v2/K.json` 对应 `2448 x 2048`
2. `test.py` 默认把结果打印出来
   - `dom_path`
   - `dsm_path`
   - `debug_overlay_path`
   - `hit_lonlat`
   - `dsm_indices`
3. README 补充了更多运行示例

### 方法上该怎么理解

如果只谈“crop 方法本身”，`crop_v2` 不能算一套新方法，更准确的说法是：

- 它是 `crop` 的另一个配置版本 / 使用版本

## `crop_wgs84_0329` 方法分析

### 方法核心变化

这一版和前两版最大的区别是：

- 主流程不再以“四个角点和 DSM 的真实射线交点”作为裁剪形状的直接来源
- 而是先在一个参考高度平面上构造几何框，再叠加 DSM mask，再做二阶段旋转裁边

这是方法思想上的明显变化。

### 第一阶段：先构造 bbox 区域

第一阶段的逻辑可以概括成：

1. 先计算整张 DSM 的一个代表高度 `area_minZ`
2. 把图像四角和图像中心投影到 `area_minZ` 这个固定高程平面
3. 得到一个投影四边形
4. 在局部 ENU 平面上分析这个四边形的边方向
5. 找到最短且近似平行的一组边
6. 用图像中心点、短边方向、图像宽高比，重建一个“斜向矩形”
7. 用这个斜向矩形去地图上截取 AABB 窗口
8. 在窗口内部生成 polygon mask
9. 再把 DSM 有效区和 polygon mask 合并

这里最关键的是：

- 它不是直接拿四边形本身做透视 warp
- 而是“基于四边形的方向信息，重新构造一个更规则的旋转矩形”
- 并且第一阶段主流程里，旧版射线参数 `num_samples / t_near / t_far` 虽然接口还在，但已经不是主流程控制变量

### `area_minZ` 和前两版的区别

前两版的兜底高度是：

- DSM 有效且大于 0 的最小值

而 `crop_wgs84_0329` 第一阶段主流程使用的是：

- DSM 有效值的中位数 `median`

这代表它不是在找“最低平面”，而是在找一个更稳的“参考高度层”。

这样做的含义是：

- 对异常低值、空洞值更不敏感
- 更适合作为“先估计 footprint 方向和范围”的参考平面
- 但它不是角点真实落地高度，因此第一阶段几何框更偏“结构化估计”而不是“真实交点”

### 第二阶段：按 yaw 旋转并裁边

`crop_wgs84_0329` 多了一整套第二阶段 pipeline：

1. 第一阶段先得到 `bbox_img / bbox_points / bbox_mask`
2. 再根据 `yaw_processed` 对三者同步旋转
3. 根据旋转后的 mask 重新裁边
4. 得到最终输出 `final_img / final_points / final_mask`

也就是说，它是一个两阶段流程：

1. 先得到带 mask 的候选框
2. 再做朝向对齐和紧裁剪

这一点和前两版“一步透视拉正后直接输出”完全不同。

### 点云生成方式也变了

前两版的点云生成路径是：

- 透视 warp 之后得到 `lon_warp / lat_warp / h_warp`
- 再统一转 ECEF

`crop_wgs84_0329` 的点云生成方式则是：

1. 在裁剪窗口内直接取 DSM 的原始高程网格
2. 用地图栅格的 `(lon, lat)` 网格和 DSM 高程构造 ECEF 点云
3. 用组合 mask 把无效区域清零

也就是说：

- 它保留了裁剪窗口内部原始地图网格的结构
- 不是对四边形内部做透视拉伸后的规则重采样

### mask 是这一版的重要新增能力

前两版没有显式的有效区域 mask。

而这一版同时维护：

- polygon mask：旋转矩形外部区域无效
- valid DSM mask：DSM 空洞或异常高程无效
- combined mask：两者叠加后的最终有效区域

这带来的直接好处是：

- 无效区域不会混入点云
- 图像和点云能保持严格同步裁切
- 后续网络或配准流程更容易只关注有效区域

### 这一版还保留了旧接口兼容层

`crop_wgs84_0329` 里仍然保留了：

- `ray_casting.py`
- `crop_dsm_dom_point(...)`

但它们在主测试入口里已经不是主要路径。

真正被 `test.py` 调用的是：

- `run_test_0310_style_pipeline(...)`

因此这版更准确的定位是：

- 一套兼容旧接口、但主流程已经改成“两阶段 bbox + rotate + mask”的新方案

### 优点

- 规则更强，输出区域更稳定
- 通过中心点和短边方向重建旋转矩形，更接近“规范化参考块”
- 显式维护 mask，适合后续几何和学习流程
- 第二阶段按 yaw 对齐后，输出方向更统一
- 对 DSM 空洞和异常值处理更明确

### 局限

- 第一阶段不是严格基于真实 DSM 交点得到 footprint，而是基于参考平面估计
- 在起伏特别大、楼体特别高、视角特别斜时，第一阶段矩形可能和真实可见范围有偏差
- 它更像“稳定生成参考块”的方法，而不是“尽量精确追踪视锥实际落地形状”的方法

## 三者方法差异总表

| 对比项 | `crop` | `crop_v2` | `crop_wgs84_0329` |
|---|---|---|---|
| 核心算法类型 | 四角点射线求交 + 透视拉正 | 与 `crop` 完全相同 | 参考高度平面投影 + 旋转矩形 + mask + 二阶段旋转 |
| 是否真实使用射线和 DSM 求交作为主流程 | 是 | 是 | 主流程不是 |
| 角点策略 | 原始角点 + 向中心缩 1/8 + 向中心缩 1/4 | 相同 | 主流程直接用四角和中心点投到参考平面 |
| 区域形状来源 | 4 个 DSM 命中点形成的四边形 | 相同 | 由中心点、短边方向、宽高比重建的旋转矩形 |
| 裁剪结果规则化方式 | 透视变换 `warpPerspective` | 相同 | 先裁 AABB，再用 mask 约束，之后按 yaw 旋转并再裁 |
| 点云生成方式 | warp 后的经纬高转 ECEF | 相同 | 裁剪网格直接转 ECEF，并用 mask 清零无效区 |
| mask 输出 | 无 | 无 | 有 |
| DSM 无效值处理 | 角点求交时过滤，最终输出无显式 mask | 相同 | 从主流程就显式构建有效 DSM mask |
| fallback / 参考高度 | 最小有效且 `>0` 的 DSM 高度 | 相同 | 主流程用有效 DSM 的中位数作为参考高度 |
| `num_samples/t_near/t_far` 是否直接影响主流程 | 是 | 是 | 主入口主流程基本不使用，接口主要为兼容保留 |
| 输出阶段数 | 单阶段 | 单阶段 | 两阶段 |
| 输出内容丰富度 | 基础输出 | 基础输出 + 更多打印 | `bbox_*` + `final_*` + `mask` + 调试图 |
| 方法本质是否独立 | 是 | 否，实质是 `crop` 的同法版本 | 是 |

## 三者相同点总表

| 相同点 | 说明 |
|---|---|
| 输入位姿格式一致 | 都使用 `[lon, lat, alt, roll, pitch, yaw]` |
| 内参处理一致 | 都会先 `normalize_K` |
| 外参构造一致 | 都通过同一个 `transform_colmap_pose_intrinsic(...)` 转 ECEF `pose_w2c` |
| 世界坐标系一致 | 内部核心三维坐标都是 ECEF |
| 地图前提一致 | 都假设 DSM / DOM 是 WGS84 栅格 |
| 最终目标一致 | 都是从 DOM / DSM 中裁出与相机视角对应的参考区域 |

## 如果按“适用场景”来理解

### 更偏几何真实落地范围

优先考虑：

- `crop`
- `crop_v2`

原因：

- 它们的起点是“角点真实射线打到 DSM 哪里”
- 更强调视锥边缘和地表交点的几何对应关系

### 更偏稳定、规则、方便后处理

优先考虑：

- `crop_wgs84_0329`

原因：

- 输出结构更规范
- 带 mask
- 带 bbox 和 final 两阶段结果
- 更适合后续做配准、学习、批处理和统一方向输出

## 最终结论

### 1. `crop` 和 `crop_v2`

这两者在“方法上”没有本质差别。

最准确的描述是：

- 同一套四角点射线求交 + 透视拉正方法
- `crop_v2` 只是换了默认参数和测试输出表现

### 2. `crop_wgs84_0329`

这是和前两者真正不同的一套方法。

它的核心变化不是小修小补，而是裁剪思想变了：

- 从“角点真实交点驱动的单阶段透视裁剪”
- 变成了“参考平面估计 footprint + 旋转矩形 + mask + yaw 对齐的两阶段裁剪”

### 3. 所以严格来说

如果你说“我有三种 crop 方法”，从入口文件角度可以这么说；
但从核心算法角度，更准确的结论是：

- 你手上其实是“两种方法、三个入口”

## 关键代码定位

下面这部分是为了后续你自己回源码时更快定位。

### `crop` / `crop_v2` 关键位置

| 功能 | 位置 |
|---|---|
| 入口参数解析与调用 | `crop/test.py` / `crop_v2/test.py` |
| 统一入口 `generate_ref_map` | `crop/crop/proj2map.py`、`crop_v2/crop/proj2map.py` 中 `generate_ref_map(...)` |
| 四角点候选构造 | `build_corner_candidate_uvs(...)` |
| 射线与 DSM 求交 | `ray_cast_to_wgs84_dsm(...)` |
| fallback 高度计算 | `compute_min_valid_dsm_height(...)` |
| 四边形透视拉正 | `cv2.getPerspectiveTransform(...)` + `cv2.warpPerspective(...)` |
| 位姿从 WGS84 转 ECEF 外参 | `transform_colmap_pose_intrinsic(...)` |

### `crop_wgs84_0329` 关键位置

| 功能 | 位置 |
|---|---|
| 入口参数解析 | `crop_wgs84_0329/test.py` |
| 主入口 | `crop_wgs84_0329/crop/pipeline.py` 中 `run_test_0310_style_pipeline(...)` |
| 第一阶段 bbox 生成 | `crop_wgs84_0329/crop/proj2map.py` 中 `generate_ref_map(...)` |
| 参考高度计算 | `crop_wgs84_0329/crop/proj2map.py` 中 `_compute_area_min_z(...)` |
| 四角与中心点投影到参考平面 | `pixel_to_world(...)` |
| 根据短边重建旋转矩形 | `find_short_parallel_edge(...)` + `build_rotated_rectangle_from_center_and_edge(...)` |
| polygon mask 构造 | `create_polygon_mask(...)` |
| DSM 有效区域约束 | `_valid_dsm_mask(...)` |
| 第二阶段旋转和裁边 | `rotate_all_with_mask(...)` |
| 与旧接口兼容的包装层 | `crop_dsm_dom_point(...)` |

### 一眼看清主调用链

`crop` / `crop_v2`：

`test.py`
-> `generate_ref_map(...)`
-> `transform_colmap_pose_intrinsic(...)`
-> `crop_dsm_dom_point(...)`
-> `ray_cast_to_wgs84_dsm(...)`
-> 角点命中
-> `warpPerspective(...)`
-> 输出

`crop_wgs84_0329`：

`test.py`
-> `run_test_0310_style_pipeline(...)`
-> `generate_ref_map(...)`
-> `crop_dsm_dom_angle(...)`
-> 参考平面投影
-> 旋转矩形 + mask
-> `rotate_all_with_mask(...)`
-> 输出

---

如果后面你需要，我可以继续把这份文档补成“逐函数、逐行号”的源码对照版。
