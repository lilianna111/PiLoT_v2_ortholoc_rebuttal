# PiLoT_v55 项目整体 Pipeline 总览

## 1. 项目目标

这个项目的核心目标是：

- 输入一段 query 图像序列
- 结合地图侧 DOM/DSM
- 对每一帧 query 做参考区域裁剪、特征对齐和位姿优化
- 输出每一帧的最终定位结果

整个系统不是单纯做图像检索，也不是单纯做渲染配准，而是：

- 先从地图中构造一张和当前位姿相关的参考图 `ref`
- 再把参考图上的 2D 点、对应的 3D 点和 query 图上的投影特征联系起来
- 最后用 LM 类优化器做位姿 refinement


## 2. 项目级输入

运行主程序时，项目依赖的输入可以分成 6 类。

### 2.1 配置输入

来自 `configs/*.yaml`，主要定义：

- checkpoint 路径
- query 相机内参
- query 图像原始尺寸与缩放策略
- 优化器配置
- 数据集根目录
- 数据集名称
- padding、候选位姿数量等运行参数

### 2.2 Query 图像序列

主程序会读取某个序列目录下的所有 query 图像。

特点：

- 支持 `.png`、`.jpg`、`.JPG`
- 对时间戳命名的图像会按数值顺序排序
- 会在读取时按配置把 query 缩放到网络使用的分辨率

### 2.3 Query 位姿文本

主程序会读取 `poses/<dataset_name>.txt`，用于：

- 取第一帧作为初始位姿
- 构建整段序列的 GT/先验位姿字典
- 最终做评估

每一行的语义是：

- 图像名
- 经度、纬度、高程
- roll、pitch、yaw

### 2.4 地图输入

地图侧主要是两份 GeoTIFF：

- DOM：正射影像，提供参考外观
- DSM：高程图，提供参考 3D

系统运行时会把这两份大地图一次性读入内存，然后对每一帧做局部 crop。

### 2.5 相机内参输入

crop 阶段单独使用一份 `K.json`，里面包含：

- 相机内参矩阵 `K`
- 原始图像宽高

它用于把 query 相机视锥投到地图上。

### 2.6 可选先验输入

主程序还支持两类附加先验：

- 深度 txt
- 角度 txt

它们不参与 crop，但会在定位优化时作为深度/姿态约束。


## 3. 项目级输出

主程序的输出主要有 3 类。

### 3.1 每帧 crop 可视化图

运行 `main.py` 时，会把每一帧最终送入定位的参考图保存出来，便于检查 crop 效果。

### 3.2 最终位姿结果

主程序结束后会生成一个结果 txt。

每一行对应一帧 query，内容包括：

- query 图像名
- 最终经纬高
- 最终姿态角

### 3.3 评估结果

主程序最后会用估计位姿和 GT 位姿做误差评估。


## 4. 项目整体主流程

主程序可以概括成两条并行链：

1. 参考图构造链
2. 定位优化链

两条链通过队列连接。

高层顺序如下：

1. 读取配置、query 列表、GT 位姿、相机参数
2. 初始化地图缓存
3. 用初始位姿为第一帧 crop 出参考图和参考 3D
4. 把参考图、参考点、采样结果送到定位线程
5. 定位线程用 query 图和参考数据做优化
6. 得到当前帧最终位姿
7. 把该位姿回送给 crop 线程，作为下一帧参考区域生成的输入
8. 循环直到整段序列结束

所以这个系统本质上是一个：

- 上一帧估计结果驱动下一帧参考构造
- 当前帧 query 与当前帧 ref 做局部精定位

的时序定位系统。


## 5. Crop 模块在整个系统中的位置

crop 不是一个独立的“前处理小工具”，而是整个定位系统的前半段。

它的任务不是只裁一张图，而是为后续定位准备三类核心数据：

- `render_frame`：参考图像
- `Points_3D_ECEF`：参考图上采样点对应的 3D 点
- `p2d_r`：这些 3D 点在参考图上的 2D 像素位置

换句话说，crop 的目标不是“裁图好看”，而是：

- 为后面的特征残差和投影残差建立 2D-3D-外观的对应关系


## 6. Crop 模块详细 Pipeline

当前主程序里实际使用的是 `crop/` 这一套方法。

### 6.1 Crop 输入

单帧 crop 的输入包括：

- 当前帧先验位姿
- query 相机内参
- query 原始图像宽高
- DSM 大地图
- DOM 大地图

### 6.2 位姿变换

先把输入的：

- 经度、纬度、高程
- roll、pitch、yaw

转换成相机在世界坐标中的外参表示。

这一步的目的，是为后面的“图像角点射线与 DSM 求交”做准备。

### 6.3 四个角点射线求交

crop 使用 query 图像四个角点来定义参考区域。

处理方式是：

1. 取 query 图像四个角点
2. 每个角点各自发出一条射线
3. 在 DSM 上寻找该射线与地表的交点

如果角点直接命中失败，还会尝试：

- 向图像中心缩一点后的候选角点

如果仍然失败，则使用一个保底高度做兜底。

### 6.4 无效值处理

当前这套 crop 对 DSM 无效值的处理原则是：

- 地图范围外无效
- `NaN` / `Inf` 无效
- `nodata` 无效
- 明显异常哨兵值无效

注意：

- `DSM < 0` 不再被当作无效值
- 所以海面等负高程区域会保留

### 6.5 构造地面四边形

四个角点落到 DSM 后，会在地图上形成一个四边形。

这个四边形代表：

- 当前 query 视场在地图上的参考区域

### 6.6 外接框裁剪

系统不会直接在整张大地图上处理，而是先取这个四边形的最小外接矩形区域：

- 从 DOM 上裁一个局部 patch
- 从 DSM 上裁一个局部 patch

### 6.7 透视规整

当前 `crop` 方法会继续做一步透视规整：

- 把地图上的四边形通过透视变换拉成规则矩形

同时参与这个变换的不是只有图像，还包括：

- DOM 颜色
- DSM 高程
- 经纬度网格

这样得到的最终 crop 结果是：

- 一张规整后的参考图
- 一份和这张参考图逐像素对应的 3D 点阵

### 6.8 3D 点生成

在透视规整后的网格上，系统会把：

- 经度
- 纬度
- 高程

统一转换成 ECEF 坐标，得到每个像素对应的 3D 点。

所以当前 crop 的直接产出不是深度图，而是：

- 一张参考图
- 一张逐像素 3D 点图

### 6.9 Crop 输出

单帧 crop 的主要输出是：

- `dom_warp`：参考图像
- `xyz_ecef`：参考图逐像素对应的 ECEF 3D 点

辅助输出还包括：

- 四个角点在地图上的位置
- 命中的经纬高
- 使用过的角点候选
- crop bbox
- homography


## 7. Main 中对 Crop 结果的后处理 Pipeline

crop 的直接输出不会立刻送入优化器，中间还有一层整理。

### 7.1 有效点筛选

系统会先从 `xyz_ecef` 中筛掉明显无效点。

当前主规则是：

- 3D 坐标必须是有限值

### 7.2 尺寸对齐

当前主程序会把 crop 结果调整到参考相机使用的尺寸。

参与同步处理的有：

- 参考图
- 3D 点图
- 有效区域 mask

### 7.3 Padding

如果配置里开启 padding，系统会继续把尺寸补到网络友好的大小。

注意这一步不会让 padding 区参与采样，因为：

- mask 的 padding 区会被置为无效

### 7.4 参考点采样

系统不会把整张参考图上的所有像素都拿去优化，而是：

- 只在有效区域内随机采样一批点

采样结果包括：

- 采样点的 3D 坐标
- 这些点在参考图上的 2D 坐标
- 对应的可见性标记

这一步之后，后端真正拿到的核心参考数据就变成：

- `render_frame`
- `Points_3D_ECEF`
- `p2d_r`
- `visible_r`


## 8. 参考数据与 Query 数据如何进入定位

定位线程每一帧会拿到两部分数据：

### 8.1 Query 侧数据

- 当前帧 query 图像
- query 相机参数
- 当前帧初始位姿候选

### 8.2 Reference 侧数据

- crop 得到的参考图 `render_frame`
- 采样后的参考 3D 点 `Points_3D_ECEF`
- 参考图上的 2D 坐标 `p2d_r`
- 参考点有效性 `visible_r`

此外还会带一些几何辅助量：

- render 侧位姿
- 初始候选位姿集合
- 去中心化偏移量
- 可选深度和角度先验


## 9. 定位模块详细 Pipeline

### 9.1 初始候选位姿生成

定位并不是只从一个初始位姿开始，而是会围绕当前 query 的先验位姿生成一批候选。

候选的来源包括：

- 姿态方向扰动
- 空间位置扰动

这样做的目的是：

- 提高优化对初值误差的容忍度

### 9.2 参考 3D 点坐标整理

采样得到的 `Points_3D_ECEF` 会做两层数值整理：

- 减去全局 origin
- 再减去局部中心偏移

这一步不是改几何意义，而是为了：

- 改善数值稳定性
- 让优化器更容易工作

### 9.3 Query 与 Ref 特征提取

系统会分别对：

- query 图
- ref 图

做 dense feature extraction。

这里不是只提采样点的特征，而是：

- 先提整张特征图
- 后续再按坐标去插值取特征

### 9.4 参考点筛选

在 ref 特征图生成后，系统还会根据置信度图对参考点再做一次筛选。

目的：

- 去掉低质量点
- 保留更稳定的参考观测

### 9.5 构造参考特征

根据 `p2d_r`，在 ref 特征图上取出每个参考点的特征向量。

这一步得到：

- 每个参考点在 ref 侧的特征描述子

### 9.6 把参考 3D 点投到 Query

对每一个候选 query 位姿，系统会：

1. 把参考 3D 点变换到 query 相机坐标系
2. 投影到 query 图像平面
3. 得到这些点在 query 图上的位置

### 9.7 构造 Query 特征

在 query 特征图上，按投影后的 2D 位置取出对应特征向量。

这样每个参考点在 query 侧也有了一个特征描述子。

### 9.8 计算投影引导的特征误差

这个系统里最核心的并不是“直接像素投影误差”，而是：

- 先用当前候选位姿把参考 3D 点投到 query 图上
- 再在该投影位置提取 query 特征
- 与参考图上对应点的 ref 特征做比较

所以它本质上是：

- 一个由 3D 投影位置决定的特征一致性误差

可以把它理解成“投影引导的特征误差”：

- 投影这一步决定 query 上去哪里取特征
- 特征差这一步决定当前位姿是不是让同一物理点对齐上了

单个参考点的误差链条是：

1. 参考图上先有一个采样点
2. 这个采样点绑定一个真实 3D 点
3. 在某个候选位姿下，这个 3D 点会投影到 query 图上的某个位置
4. 系统在这个位置上取 query 特征
5. 用 query 特征减去 ref 特征，得到该点的主误差

因此这里的“投影”不是单独作为最终损失，而是：

- 作为把 3D 点映射到 query 图的几何步骤
- 决定 query 特征从哪里取
- 再由 query/ref 特征差形成主残差

更具体地说，每个参考点都会形成一条约束链：

- `ref` 上有一个 2D 点
- 这个 2D 点绑定一个 3D 点
- 这个 3D 点在当前候选 query 位姿下会投到 query 上某个位置
- 系统比较这两个位置上的特征是否一致

如果候选位姿是对的，那么：

- 3D 点投到 query 上的位置应该接近真实成像位置
- 从 query 上取到的特征应该和 ref 上该点的特征更接近

如果候选位姿是错的，那么：

- 投影位置会偏
- query 侧取到的就不是同一个物理点附近的特征
- 特征残差会变大

所以优化器实际上是在用：

- “位姿变化会如何改变 3D 点在 query 上的投影位置”

来间接最小化 query/ref 之间的特征不一致。

所以当前项目里，如果口头上说“投影误差”，更准确的理解应该是：

- 不是传统 BA 里那种 2D 几何重投影像素误差
- 而是通过 3D 投影来驱动的特征残差

### 9.9 计算深度误差与角度误差

除了主残差，系统还可以额外计算两类先验误差。

#### 9.9.1 深度误差

深度误差的核心思想是：

- 取当前候选位姿下，相机主视线中心点对应的 3D 位置
- 查询这个位置在 DSM 上的地图高程
- 比较“当前估计得到的中心点高度”和“DSM 高度”之间的差

所以深度误差约束的不是整张图，而是：

- 当前位姿是否让相机中心射线落在合理的地图高度关系上

它的作用是：

- 防止位姿在高度方向或局部几何上明显漂离地图表面
- 在纹理弱区域给优化提供额外的垂向约束

#### 9.9.2 角度误差

角度误差的核心思想是：

- 把当前候选位姿转换到局部 ENU 姿态表达
- 取出其中的 roll 和 pitch
- 与外部提供的 `gt_roll`、`gt_pitch` 做比较

所以角度误差约束的是：

- 当前候选姿态是否偏离外部姿态先验太多

它不直接约束平移，而主要约束：

- 旋转中的 roll
- 旋转中的 pitch

它的作用是：

- 防止姿态在局部优化中发生不合理倾斜
- 在 query/ref 外观不稳定时，帮助姿态朝更可信的方向收敛

### 9.10 三类误差怎么合在一起

系统最终不是只看单一误差，而是把三类信息一起送进优化：

- 主项：投影引导的特征误差
- 辅助项：深度误差
- 辅助项：角度误差

它们合并时遵循两个原则。

#### 原则一：特征误差是主导项

系统首先依赖的是 query/ref 的特征对齐效果，因为：

- 它直接反映当前位姿下，3D 点投到 query 后是否真的落在正确图像位置附近
- 它是与视觉观测最直接相关的误差

#### 原则二：深度和角度是辅助项

深度和角度不会替代主残差，而是：

- 在特征误差之外提供额外约束
- 当主残差不够稳定时帮助收敛
- 当先验与主残差明显冲突时，系统会对这些辅助项做抑制，而不是让它们无条件主导结果

从优化角度看，三类误差不是简单生硬地直接相加，而是会经历：

- 有效性检查
- 鲁棒处理
- 自适应缩放
- 再共同进入当前迭代的更新步骤

也就是说，系统并不是固定写死：

- “特征误差乘一个常数”
- “深度误差乘一个常数”
- “角度误差乘一个常数”

而是会根据当前候选位姿下各项误差的量级、有效性和一致性，动态决定：

- 这一轮哪些误差可信
- 哪些误差该弱一些
- 哪些误差可以更强地参与更新

### 9.11 位姿优化

最后，系统会基于多候选位姿做迭代优化。

这个阶段可以拆成 4 步理解。

#### 第一步：并行评估多组初始候选

系统不会只拿一个初始位姿直接优化，而是会先构造一批候选位姿。

每个候选位姿都会各自计算：

- 投影引导的特征误差
- 对应雅可比
- 深度误差及其导数
- 角度误差及其导数

这样做的目的是：

- 避免单一初值离真值太远时直接陷入错误局部极值
- 让系统在一个小范围候选集中寻找更可靠的收敛起点

#### 第二步：对每个候选做局部迭代更新

对每个候选位姿，优化器都会重复下面的过程：

1. 根据当前位姿计算所有残差
2. 计算残差对位姿的雅可比
3. 把特征项、深度项、角度项共同线性化
4. 求解一个当前迭代的位姿增量
5. 用这个增量更新当前位姿

这一步本质上就是：

- 把非线性的位姿优化问题
- 近似成当前点附近的局部线性问题
- 再不断迭代逼近更优解

更直观地说：

- 特征误差决定“图像上哪里没对齐”
- 深度误差决定“高度关系是否合理”
- 角度误差决定“姿态倾斜是否合理”

优化器在每一轮都会综合这三种信息，求出一个新的位姿修正方向。

#### 第三步：在迭代过程中筛掉明显不可靠的候选

优化完成后，系统不会盲目接受所有候选结果，而是会继续检查：

- 是否优化失败
- 优化后的平移变化是否过大
- 优化后的旋转变化是否过大
- 最终 loss 是否足够低

也就是说，系统会先剔除：

- 发散的候选
- 虽然数值上收敛，但明显不合理的候选

#### 第四步：从剩余候选中选最终结果

在保留下来的候选中，系统会选择：

- 最终整体 loss 最小
- 同时满足变化量约束

的那一个，作为当前帧最终位姿。

所以这一阶段的输出不是“某一次迭代的中间值”，而是：

- 多候选局部优化之后
- 经过约束筛选
- 再做最优选择

得到的最终 pose。

从 pipeline 角度看，这一步的意义是：

- 借助多候选和局部优化，提升对初值误差的鲁棒性
- 借助先验和筛选规则，降低明显错误解被选中的概率


## 10. 为什么 Crop 能支持后续定位

从整个项目设计来看，crop 的作用不是简单裁出“看起来像”的一张图，而是为优化器提供一组三元组：

- 参考图像
- 参考图上的 2D 点
- 与这些 2D 点对应的 3D 点

后续定位真正依赖的是这组三元组的对应关系。

所以从 pipeline 角度看，crop 的核心价值是：

- 提供可用于投影残差优化的参考观测


## 11. 独立 Crop 调试流程

项目里也保留了 `crop/test.py` 这种单独调试入口。

它的用途是：

- 不跑整套定位
- 单独验证某个 pose 在某个 DOM/DSM 上 crop 出来的结果

独立调试时的输入是：

- DSM 路径
- DOM 路径
- 单帧 pose
- `K.json`

独立调试时的输出是：

- crop 图
- crop 对应的 3D 点文件
- 可视化调试图

所以它更像：

- crop 模块的单元测试入口

而 `main.py` 是：

- 整个项目的完整时序定位入口


## 12. 项目中的关键模块分工

### 12.1 `main.py`

负责项目总控：

- 读配置
- 读序列
- 启动 crop 线程和定位线程
- 串联输入输出
- 保存最终结果
- 做评估

### 12.2 `crop/`

负责地图侧参考构造：

- 位姿变换
- 四角点投射
- DSM/DOM 裁剪
- 参考图生成
- 3D 点图生成

### 12.3 `pixloc/utils/get_depth.py`

负责把参考点和位姿信息整理成优化器真正可用的形式：

- 初始候选位姿生成
- 3D 点坐标去中心化
- render/query 位姿转换

### 12.4 `pixloc/localization/`

负责定位主流程：

- 特征提取
- 点筛选
- 残差构造
- 优化与结果选择

### 12.5 `pixloc/pixlib/geometry/`

负责几何与优化底层：

- 投影
- 插值
- 残差
- 雅可比
- 优化迭代


## 13. 一条完整帧的最终 Pipeline

可以把单帧流程压缩成下面这条链：

1. 读取当前 query 图像
2. 取当前帧先验位姿
3. 用先验位姿在 DOM/DSM 上做 crop
4. 得到参考图和参考 3D 点图
5. 过滤无效点并采样参考点
6. 构造 `render_frame / Points_3D_ECEF / p2d_r`
7. 生成 query 初始候选位姿
8. 提取 query/ref 特征图
9. 在 ref 上按 `p2d_r` 取参考特征
10. 用候选位姿把参考 3D 点投到 query
11. 在 query 上取投影位置的特征
12. 计算特征残差与先验残差
13. 迭代优化得到最终位姿
14. 把当前帧结果作为下一帧参考构造的输入
15. 保存最终结果并继续下一帧


## 14. 项目当前最重要的系统特征

从 pipeline 视角看，这个项目有 4 个鲜明特点。

### 14.1 它是序列式定位，不是单帧独立定位

每一帧的 crop 和初始化，都受上一帧结果影响。

### 14.2 它是“地图裁剪 + 局部对齐”模式

不是全图检索后直接 PnP，而是：

- 先从地图中构造局部参考
- 再做局部位姿 refinement

### 14.3 它的参考端是地图生成的，不是预先拍好的数据库图

ref 来自 DOM/DSM，而不是传统 SfM 数据库图像。

### 14.4 它的优化核心是特征残差，不是 RGB 残差

真正进入优化的是：

- 特征图中的对应点特征差

而不是直接比较原始像素值。


## 15. 一句话总结

这个项目的完整 pipeline 可以概括为：

- 用先验位姿在 DOM/DSM 上构造局部参考图和参考 3D 点
- 在 query 与 reference 之间建立 2D-3D-特征对应
- 用这些对应关系和附加先验做局部位姿优化
- 逐帧迭代完成整段序列的高精度定位
